<thlogic>
    <attr sel="#headId"
          th:replace="~{fragments/head :: headFragment}"/>
    <attr sel="#play-page"
          th:href="@{__${T(example.spring.multimodule.util.GameMapping).PLAY}__}"
          th:text="Play"/>
    <attr sel="#footerId"
          th:replace="~{fragments/footer :: footer}"/>
</thlogic>